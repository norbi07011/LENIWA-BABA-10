# Netlify configuration for Vite React app
[build]
  command = "npm run build"
  publish = "dist"

[context.production]
  branch = "master"

[dev]
  command = "npm run dev"
  port = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200